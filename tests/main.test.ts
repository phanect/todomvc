import { afterEach, beforeEach, expect, test } from "vitest";

beforeEach(() => {
  // Write code
});

afterEach(() => {
  // Write code
});

test("test", async () => {
  expect(1).toBe(1);
}, 50000);
